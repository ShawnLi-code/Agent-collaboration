# Projects

每个项目在这里建立一个独立文件夹。

推荐命名：

```text
projects/<project-slug>/
```

例子：

```text
projects/reading-assistant/
projects/personal-knowledge-base/
projects/web-app-redesign/
```

不要把多个无关项目混在同一个目录。每个项目都有自己的 `PROJECT.md` 和 `.agent-work/`。

